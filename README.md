# ELITE-MD (public build)

This is an obfuscated distribution build. Source is maintained privately;
this repo contains only the generated build output plus the assets needed
to deploy the bot.

## Deploy
```
npm install
cp .env.example .env
npm start
```
